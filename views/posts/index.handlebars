<div class="card">
    <div class="card-body">
        <h2>{{postagem.title}}</h2>
        <hr>
        <small>Data da publicação:{{postagem.date}}</small><br>
        <hr>
        <p>{{postagem.content}}</p>
    </div>
</div>
